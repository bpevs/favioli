export const OPTIONS_ROUTE = {
  BYPASSED_SITES: "BYPASSED_SITES",
  FAVICONS: "FAVICONS",
  SETTINGS: "SETTINGS",
};

export const EMOJI_SIZE = 256; // Any larger causes problems in Chrome

export const DEFAULT_OVERRIDES = [];
